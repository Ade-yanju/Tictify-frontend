import {useEffect,useState} from "react";import{getToken}from"../../services/authService";import AdminShell from"../../components/AdminShell";
export default function AdminFeedback(){const[data,setData]=useState([]),[loading,setLoading]=useState(true),[filter,setFilter]=useState("ALL");useEffect(()=>{fetch(`${import.meta.env.VITE_API_URL}/api/feedback/admin`,{headers:{Authorization:`Bearer ${getToken()}`}}).then(r=>r.json()).then(x=>setData(Array.isArray(x)?x:[])).finally(()=>setLoading(false))},[]);async function status(x,s){const r=await fetch(`${import.meta.env.VITE_API_URL}/api/feedback/admin/${x._id}`,{method:"PATCH",headers:{"Content-Type":"application/json",Authorization:`Bearer ${getToken()}`},body:JSON.stringify({status:s})});const n=await r.json();if(r.ok)setData(a=>a.map(v=>v._id===x._id?n:v))}const list=filter==="ALL"?data:data.filter(x=>(x.role||"guest").toUpperCase()===filter);return <AdminShell active="/admin/feedback"><style>{`.af-wrap{max-width:1040px}.af-kicker{color:#e8c96a;text-transform:uppercase;letter-spacing:2px;font-size:11px;font-weight:800}.af-wrap h1{font:800 clamp(30px,4vw,46px) Syne;margin:10px 0}.af-sub{color:#aaa7a0}.af-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:center;margin:30px 0 18px;flex-wrap:wrap}.af-filter{display:flex;gap:8px;flex-wrap:wrap}.af-filter button{border-radius:999px!important;padding:9px 15px!important}.af-filter button.active{background:#e8c96a!important;color:#111!important}.af-card{padding:22px;border-radius:18px;background:#151522;border:1px solid #ffffff12;margin:14px 0}.af-head{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap}.af-person{font-weight:800}.af-meta{color:#aaa7a0;font-size:13px;margin:7px 0 15px}.af-role{color:#e8c96a;text-transform:capitalize}.af-message{line-height:1.65;color:#f0ede8}.af-date{color:#777;font-size:12px}.af-card select{margin-top:15px}.af-empty{padding:50px 20px;text-align:center;color:#aaa7a0;background:#151522;border-radius:18px}@media(max-width:600px){.af-card{padding:18px}}`}</style><div className="af-wrap"><span className="af-kicker">Community voice</span><h1>Feedback inbox</h1><p className="af-sub">Listen, learn, and improve the Tictify experience.</p><div className="af-toolbar"><strong>{data.length} submission{data.length===1?"":"s"}</strong><div className="af-filter">{["ALL","ORGANIZER","AFFILIATE","GUEST"].map(x=><button className={filter===x?"active":""} key={x} onClick={()=>setFilter(x)}>{x[0]+x.slice(1).toLowerCase()}</button>)}</div></div>{loading?<div className="af-empty">Loading feedback…</div>:list.length===0?<div className="af-empty">No feedback matches this filter.</div>:list.map(x=><article className="af-card" key={x._id}><div className="af-head"><span className="af-person">{x.name}</span><span className="af-date">{new Date(x.createdAt).toLocaleString()}</span></div><div className="af-meta">{x.email} · <span className="af-role">{x.role||"guest"}</span> · {x.category} · {x.rating||"—"}/5</div><p className="af-message">{x.message}</p><select value={x.status} onChange={e=>status(x,e.target.value)}><option>NEW</option><option>REVIEWED</option><option>RESOLVED</option></select></article>)}</div></AdminShell>}
