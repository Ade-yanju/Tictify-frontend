import {useEffect,useState} from "react";import{getToken}from"../../services/authService";import OrganizerShell from"../../components/OrganizerShell";
export default function OrganizerInsights(){const[e,setE]=useState([]),[r,setR]=useState(null);useEffect(()=>{const h={Authorization:`Bearer ${getToken()}`};Promise.all([fetch(`${import.meta.env.VITE_API_URL}/api/organizer/events/stats`,{headers:h}).then(x=>x.json()),fetch(`${import.meta.env.VITE_API_URL}/api/organizer/referrals`,{headers:h}).then(x=>x.json())]).then(([a,b])=>{setE(Array.isArray(a)?a:[]);setR(b)})},[]);const sold=e.reduce((n,x)=>n+(x.ticketsSold||0),0),scan=e.reduce((n,x)=>n+(x.ticketsScanned||0),0),rev=e.reduce((n,x)=>n+(x.revenue||0),0),rate=sold?Math.round(scan/sold*100):0;return <OrganizerShell active="/organizer/insights"><style>{`.ins-wrap{max-width:1050px}.ins-kicker{color:#e8c96a;text-transform:uppercase;letter-spacing:2px;font-size:11px;font-weight:700}.ins-wrap h1{font:800 clamp(30px,4vw,46px) Syne;margin:10px 0}.ins-sub{color:#aaa7a0}.ins-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:14px;margin:30px 0}.ins-card{padding:22px;border:1px solid #ffffff0d;border-radius:18px;background:#151522}.ins-card small{color:#aaa7a0}.ins-card h2{font:800 28px Syne;margin-top:12px}.ins-list{border-top:1px solid #ffffff12}.ins-row{display:flex;justify-content:space-between;gap:15px;padding:18px 0;border-bottom:1px solid #ffffff12;color:#aaa7a0}.ins-row strong{color:#f0ede8}.ins-empty{padding:28px;border-radius:16px;background:#151522;color:#aaa7a0}@media(max-width:600px){.ins-row{display:block}.ins-row span{display:block;margin-top:7px}}`}</style><div className="ins-wrap"><span className="ins-kicker">Performance center</span><h1>Organizer insights</h1><p className="ins-sub">See what is working and make your next event even better.</p><section className="ins-grid">{[["Events",e.length],["Tickets sold",sold],["Revenue",`₦${rev.toLocaleString()}`],["Attendance",`${rate}%`],["Referrals",r?.referrals||0]].map(([l,v])=><article className="ins-card" key={l}><small>{l}</small><h2>{v}</h2></article>)}</section><h2>Event performance</h2>{e.length?<div className="ins-list">{e.map(x=><div className="ins-row" key={String(x.eventId)}><strong>{x.title}</strong><span>{x.ticketsSold} sold · {x.ticketsScanned} attended · ₦{Number(x.revenue||0).toLocaleString()}</span></div>)}</div>:<div className="ins-empty">Create your first event to start seeing performance insights.</div>}</div></OrganizerShell>}
